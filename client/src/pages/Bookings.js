// import { useParams } from "react-router-dom";
import { packages  } from "../mock_data/packages";
import { users  } from "../mock_data/users";


export function Bookings() {
  // const { id } = useParams();
  return <h1>Bookings for user</h1>;
}
